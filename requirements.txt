# M3-B1 — requirements (exploration et note d'identification, pas de code lourd)
# Python 3.11+

# Core data
pandas==2.2.2
numpy==1.26.4

# Faker : pour la stratégie d'anonymisation par substitution (faux noms cohérents)
# faker==25.9.1

# Visualisation
# matplotlib==3.9.2
# seaborn==0.13.2

# NLP — détection PII
# spacy==3.7.5
# click requis explicitement : depuis typer 0.13+, `typer-slim` n'embarque
# plus click, et spacy 3.7.5 (via sa CLI) en a besoin pour s'importer. Sans ce
# pin, un install frais casse sur `ModuleNotFoundError: No module named 'click'`.
# click==8.1.7

# Persistance
# pyarrow==17.0.0

# Notebook
jupyter==1.1.1
ipykernel==6.29.5

# Optionnel — décommente pour la mission étoile (bonus RAG : préparation)
# Loader fait main (PAS de LangChain), index vectoriel + embeddings 100 % locaux
chromadb==0.5.18
sentence-transformers==3.3.1
